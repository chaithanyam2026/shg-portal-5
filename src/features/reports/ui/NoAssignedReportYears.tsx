import { Typography } from "@mui/material";

export default function NoAssignedReportYears() {
  return (
    <Typography color="text.secondary">
      You can only view reports for financial years where you are president, secretary, or
      treasurer.
    </Typography>
  );
}
